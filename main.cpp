#include <iostream>
using namespace std;

int main(){
    float a;
    float b;
    float c;
    cin >> a >> b >> c;
    cout<<((b*a)/2);
    cout<<(a*a);
    cout<<(a*b);
    cout<<(((a+b)*c)/2);
    return 0;


}
